FROM python:3.11-slim

WORKDIR /app

# Gradio 5.x: sem problemas de HfFolder e sem localhost-check em Docker
RUN pip install --no-cache-dir \
    "gradio>=5.0,<6.0" \
    requests==2.32.3

COPY src/frontend/ ./src/frontend/

EXPOSE 7860

CMD ["python", "src/frontend/app.py"]
